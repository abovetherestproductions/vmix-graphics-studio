# Dependencies
node_modules/

# Live runtime data — changes per competition, not part of the codebase.
# EXCEPTION: event-config.json (layout/theme/positions) is tracked so the
# Mac's look-and-feel syncs to other machines via pull.
public/data/*.json
!public/data/event-config.json
config/current-state.json
config/history/

# Logs
logs/
*.log

# Event-specific input files
CSV Data/
manual skater list/

# macOS
.DS_Store
**/.DS_Store

# Environment / secrets (precaution)
.env
.env.*
